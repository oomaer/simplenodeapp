{
  "dependencies": {
    "babel": "^6.23.0",
    "cors": "^2.8.5",
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "mongoose": "^6.7.0"
  },
  "scripts": {
    "start": "node index.js",
    "test": "npm install inherits && mocha",
    "build": "mkdir dist && babel -s -d dist"
  },
  "devDependencies": {
    "inherits": "^2.0.3",
    "mocha": "^5.2.0",
    "ms": "^2.1.1"
  }
}
